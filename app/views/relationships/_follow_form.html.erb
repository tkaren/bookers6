  <% unless user == current_user %>
    <% if current_user.following?(user) %>
      <%= link_to "フォロー外す", relationships_path(user_id: user.id), method: :delete, class: 'btn btn-info btn-sm' %>
    <% else %>
      <%= link_to "フォローする", relationships_path(user_id: user.id), method: :post, class: 'btn btn-success btn-sm' %>
    <% end %>
  <% end %>

